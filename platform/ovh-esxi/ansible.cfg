[defaults]
host_key_checking = False
# ESXi is driven with raw over SSH; accept the host key on first contact and skip
# SSH multiplexing (ESXi's sshd is finicky about ControlMaster).
[ssh_connection]
ssh_args = -o StrictHostKeyChecking=accept-new -o ControlMaster=no
