# Copy to inventory.ini (gitignored) and set your ESXi host. The seeder reaches it
# over SSH as root using your forwarded agent (CAPI smart card) — no key on disk.
[esxi]
esxi.example.net ansible_user=root
